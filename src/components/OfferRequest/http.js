class HttpRequest {
  constructor(url) {
    this.url = url;
  }
  
  uploadFiles() {
    return new Promise(resolve => resolve());
  }

  sendRequest() {
    return new Promise(resolve => resolve());
  }
}
export default HttpRequest;